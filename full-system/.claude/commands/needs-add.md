Add items to the running needs list (`planning/needs-this-week.md`).

The user will say something like:
- "/needs-add we're out of ranch dressing and low on peanut butter"
- "/needs-add kid needs more applesauce pouches"

Instructions:
1. Add each item to `planning/needs-this-week.md`, with any brand/reason context in parentheses ("(kid's snacks)", "(lunches)").
2. Don't duplicate — if the item is already on the list, leave it.
3. Skip anything `family/household-rules.md` says never to buy.

Confirm what was added in one line.
